/** Staff-created and email deposit checkout sessions. */
export const STAFF_DEPOSIT_CHECKOUT_EXPIRY_SECONDS = 12 * 60 * 60;

/** Public self-serve booking holds the slot while unpaid. */
export const PUBLIC_BOOKING_DEPOSIT_CHECKOUT_EXPIRY_SECONDS = 60 * 60;
